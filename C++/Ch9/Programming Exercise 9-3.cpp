#include <iostream> // Include the standard input/output stream library (for cin, cout).
#include <string>   // Include the string library (required for using the string data type in the struct).
using namespace std; // Use the standard namespace to simplify code (e.g., using string instead of std::string).

// Define a structure (custom data type) named menuItemType to hold menu item details.
struct menuItemType
{
    string menuItem; // Member to store the name/description of the menu item (text).
    double menuPrice; // Member to store the price of the menu item (floating-point number).
};

// Main function where program execution begins.
int main ()
{
    // The body of the main function is currently empty, meaning the program does nothing yet.
    return 0; // Indicate that the program finished successfully.
}
